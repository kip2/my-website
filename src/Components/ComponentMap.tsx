import LandOfLispPage from "./Page/About/History/HistoryPage/LandOfLispPage"

interface ComponentMap {
    [key: string]: React.ElementType
}

const componentMap: ComponentMap = {
    // Fill in the component name if you need to add a page
    LandOfLispPage: LandOfLispPage
}

export default componentMap